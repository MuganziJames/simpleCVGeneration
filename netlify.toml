[build]
  publish = "frontend"

# Proxy /api/* → Railway backend (keeps the same domain, no CORS issues)
# Replace the URL below with your actual Railway URL after deploying
[[redirects]]
  from   = "/api/*"
  to     = "https://simplecvgeneration-production.up.railway.app/:splat"
  status = 200
  force  = true
